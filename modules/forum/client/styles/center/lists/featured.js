import {Styles} from 'material-ui';
const {Colors} = Styles;

export default {
  wrapper: {
    backgroundColor: Colors.white
  },

  header: {
    color: Colors.grey500,
    paddingTop: '1.25rem',
    paddingLeft: '1.25rem'
  }
}
